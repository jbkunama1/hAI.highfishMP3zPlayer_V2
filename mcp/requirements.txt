# MCP server for the MP3z music library
mcp>=1.29.0,<2   # <2 keeps mcp.server.fastmcp import (FastMCP) valid
mutagen>=1.47.0
python-multipart>=0.0.27   # explicit lower bound: fixes header-parsing DoS (CVE-2026-42561 etc.)